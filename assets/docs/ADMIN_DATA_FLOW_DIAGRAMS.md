# Admin Dashboard - Data Flow Diagrams

---

## 📊 Overview Page Data Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    OVERVIEW PAGE LOAD                           │
└─────────────────────────────────────────────────────────────────┘

User Clicks "Overview" in Sidebar
         ↓
navigateToAdminPage('overview')
         ↓
loadAdminPage('overview')
         ↓
loadScript('pages/overview.js')
         ↓
loadOverviewPage()
         ↓
Show Loading Spinner
         ↓
┌────────────────────────────────────────────────────────────────┐
│  PARALLEL API CALLS                                            │
├────────────────────────────────────────────────────────────────┤
│  1. GET /api/admin/stats/overview                             │
│     └─ Returns: {total_users, active_users, suspended_users,   │
│                   total_revenue}                               │
│                                                                 │
│  2. GET /api/admin/activity                                   │
│     └─ Returns: [{action, user_id, timestamp, details}, ...]   │
│                                                                 │
│  3. GET /api/admin/revenue/breakdown (async after render)     │
│     └─ Returns: {free: 0, pro: 5234.50, premium: 8500}        │
└────────────────────────────────────────────────────────────────┘
         ↓
┌────────────────────────────────────────────────────────────────┐
│  DATA PROCESSING                                               │
├────────────────────────────────────────────────────────────────┤
│  Overview:                                                     │
│  - overview?.total_users || 0                                  │
│  - overview?.active_users || 0                                 │
│  - overview?.suspended_users || 0                              │
│  - overview?.total_revenue || 0                                │
│                                                                 │
│  Activity:                                                     │
│  - Handles: Array or Object response                           │
│  - Extract: action, user_id, timestamp, details                │
│  - Format: Dates to locale string                              │
│  - Limit: First 10 items                                       │
│                                                                 │
│  Revenue:                                                      │
│  - Convert: Object to Array if needed                          │
│  - Extract: name/tier and amount/revenue                       │
│  - Format: Amount as USD currency                              │
└────────────────────────────────────────────────────────────────┘
         ↓
┌────────────────────────────────────────────────────────────────┐
│  RENDER HTML                                                   │
├────────────────────────────────────────────────────────────────┤
│  ┌─ Stats Card 1: Total Users                                 │
│  │  └─ Value: ${overview?.total_users || 0}                   │
│  │  └─ Icon: fas fa-users (Blue)                              │
│  │                                                              │
│  ├─ Stats Card 2: Active Users                                │
│  │  └─ Value: ${overview?.active_users || 0}                  │
│  │  └─ Icon: fas fa-user-check (Green)                        │
│  │                                                              │
│  ├─ Stats Card 3: Suspended Users                             │
│  │  └─ Value: ${overview?.suspended_users || 0}               │
│  │  └─ Icon: fas fa-user-slash (Orange)                       │
│  │                                                              │
│  ├─ Stats Card 4: Total Revenue                               │
│  │  └─ Value: $${total_revenue.toLocaleString(...)}           │
│  │  └─ Icon: fas fa-dollar-sign (Purple)                      │
│  │                                                              │
│  ├─ Revenue Breakdown Grid                                    │
│  │  └─ For each tier/category: Display name + amount          │
│  │  └─ Format: $5,234.50 (USD)                                │
│  │                                                              │
│  └─ Recent Activity Table                                     │
│     └─ Columns: Action | User | Timestamp | Details           │
│     └─ Rows: First 10 activities                               │
└────────────────────────────────────────────────────────────────┘
         ↓
Display Results to User
         ↓
User sees:
  • 4 stat cards with numbers and icons
  • Revenue breakdown by tier
  • Recent admin activity log
```

---

## 👥 Users Page Data Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    USERS PAGE LOAD                              │
└─────────────────────────────────────────────────────────────────┘

User Clicks "Users" in Sidebar
         ↓
navigateToAdminPage('users')
         ↓
loadAdminPage('users')
         ↓
loadScript('pages/users.js')
         ↓
loadUsersPage()
         ↓
Show Loading Spinner
         ↓
GET /api/admin/users
         ↓
┌────────────────────────────────────────────────────────────────┐
│  RESPONSE HANDLING                                             │
├────────────────────────────────────────────────────────────────┤
│  Response Format 1 (Array):                                    │
│  [{id, email, tier, is_suspended, created_at}, ...]           │
│                                                                 │
│  Response Format 2 (Paginated Object):                        │
│  {users: [...], total: 1250, page: 1, per_page: 50}           │
│                                                                 │
│  Code handles both:                                            │
│  const users = Array.isArray(response) ? response :            │
│                response?.users || [];                           │
└────────────────────────────────────────────────────────────────┘
         ↓
┌────────────────────────────────────────────────────────────────┐
│  DATA STORAGE                                                  │
├────────────────────────────────────────────────────────────────┤
│  Store in: window.allUsers = users                             │
│  Purpose: Used for search/filter functionality                 │
│  Available to: filterUsers() function                          │
└────────────────────────────────────────────────────────────────┘
         ↓
┌────────────────────────────────────────────────────────────────┐
│  RENDER TABLE                                                  │
├────────────────────────────────────────────────────────────────┤
│  For each user in users array:                                 │
│  ├─ User ID: ${user.id || user.user_id}                       │
│  │           → Truncated to 12 chars + "..."                  │
│  │                                                              │
│  ├─ Email: ${user.email || 'N/A'}                             │
│  │                                                              │
│  ├─ Status Badge:                                             │
│  │  - If is_suspended: "Suspended" (Red #f87171)              │
│  │  - Else: "Active" (Green #4ade80)                          │
│  │                                                              │
│  ├─ Tier: ${user.tier || user.subscription_tier || 'Free'}   │
│  │                                                              │
│  ├─ Joined: new Date(user.created_at || user.joined_date)    │
│  │           → .toLocaleDateString()                           │
│  │                                                              │
│  └─ Actions:                                                  │
│     - View button → Opens user details modal                  │
│     - Suspend/Activate button (depends on status)             │
└────────────────────────────────────────────────────────────────┘
         ↓
Display Users Table with Search Box
```

---

## 🔍 User Search/Filter Flow

```
User Types in Search Box
         ↓
onkeyup="filterUsers()"
         ↓
filterUsers() executes:
  1. Get search term: searchInput = input.value.toLowerCase()
  2. Get all users: window.allUsers
  3. Filter users:
     - If email.includes(searchInput) → Include
     - If user_id.includes(searchInput) → Include
  4. Render filtered results
         ↓
Table updates LIVE with matching users

Example:
  Search for "john" →
  Shows users with email containing "john" or ID containing "john"
```

---

## 👤 View User Details Flow

```
┌─────────────────────────────────────────────────────────────────┐
│              VIEW USER DETAILS MODAL                            │
└─────────────────────────────────────────────────────────────────┘

User Clicks "View" Button for User
         ↓
viewUserDetails(userId)
         ↓
Show Modal with Loading Spinner
         ↓
GET /api/admin/users/{userId}
         ↓
┌────────────────────────────────────────────────────────────────┐
│  RESPONSE DATA                                                 │
├────────────────────────────────────────────────────────────────┤
│  {                                                              │
│    id: "user_123",                                             │
│    email: "user@example.com",                                  │
│    tier: "pro",                                                │
│    is_suspended: false,                                        │
│    created_at: "2025-01-15T10:30:00Z"                          │
│  }                                                              │
└────────────────────────────────────────────────────────────────┘
         ↓
┌────────────────────────────────────────────────────────────────┐
│  RENDER MODAL FORM                                             │
├────────────────────────────────────────────────────────────────┤
│  ┌─ User ID Input (Readonly)                                  │
│  │  └─ Value: ${user.id || user.user_id}                     │
│  │                                                              │
│  ├─ Email Input (Readonly)                                    │
│  │  └─ Value: ${user.email || ''}                             │
│  │                                                              │
│  ├─ Current Tier Input (Readonly)                             │
│  │  └─ Value: ${user.tier || user.subscription_tier || 'Free'} │
│  │                                                              │
│  ├─ NEW Tier Selector (Dropdown)                              │
│  │  └─ Options: Free, Pro, Premium, Enterprise                │
│  │  └─ id="newTierSelect" (used by update function)           │
│  │                                                              │
│  ├─ Status Input (Readonly)                                   │
│  │  └─ Value: ${is_suspended ? 'Suspended' : 'Active'}        │
│  │                                                              │
│  ├─ Joined Date Input (Readonly)                              │
│  │  └─ Value: ${new Date(created_at).toLocaleString()}        │
│  │                                                              │
│  ├─ Close Button                                              │
│  │  └─ onclick="closeUserDetailsModal()"                      │
│  │                                                              │
│  └─ Update Tier Button                                        │
│     └─ onclick="updateUserTier('${userId}')"                  │
└────────────────────────────────────────────────────────────────┘
         ↓
Modal Displayed to User
```

---

## 🎯 Update User Tier Flow

```
┌─────────────────────────────────────────────────────────────────┐
│               UPDATE USER TIER FLOW                             │
└─────────────────────────────────────────────────────────────────┘

User Selects New Tier from Dropdown
         ↓
User Clicks "Update Tier" Button
         ↓
updateUserTier(userId)
         ↓
┌────────────────────────────────────────────────────────────────┐
│  GET DATA                                                      │
├────────────────────────────────────────────────────────────────┤
│  const tierSelect = document.getElementById('newTierSelect')   │
│  const newTier = tierSelect.value  // "pro", "premium", etc   │
│  const button = event.target                                   │
│  const originalHTML = button.innerHTML                         │
└────────────────────────────────────────────────────────────────┘
         ↓
┌────────────────────────────────────────────────────────────────┐
│  SHOW LOADING STATE                                            │
├────────────────────────────────────────────────────────────────┤
│  button.disabled = true                                        │
│  button.innerHTML = '<i class="fas fa-spinner"></i> Updating...'│
└────────────────────────────────────────────────────────────────┘
         ↓
PUT /api/admin/users/{userId}/tier
Content-Type: application/json

{
  tier: "premium"  // newTier value
}
         ↓
┌────────────────────────────────────────────────────────────────┐
│  SUCCESS                                                       │
├────────────────────────────────────────────────────────────────┤
│  1. button.innerHTML = '<i class="fas fa-check"></i> Updated!' │
│  2. Wait 1.5 seconds                                           │
│  3. closeUserDetailsModal()                                    │
│  4. loadUsersPage()  → Refresh entire users list               │
└────────────────────────────────────────────────────────────────┘
         ↓
User Sees:
  • "Updated!" message on button
  • Modal closes
  • Users table reloads with updated tier
```

---

## ⛔ Suspend User Flow

```
┌─────────────────────────────────────────────────────────────────┐
│               SUSPEND USER FLOW                                 │
└─────────────────────────────────────────────────────────────────┘

User Clicks "Suspend" Button
         ↓
suspendUserConfirm(userId)
         ↓
┌────────────────────────────────────────────────────────────────┐
│  CONFIRMATION DIALOG                                           │
├────────────────────────────────────────────────────────────────┤
│  Message:                                                      │
│  "Are you sure you want to suspend this user?                  │
│   They will lose access to the platform."                      │
│                                                                 │
│  Buttons:                                                      │
│  - Cancel (No)  → Exit without action                          │
│  - OK (Yes)     → Proceed with suspension                      │
└────────────────────────────────────────────────────────────────┘
         ↓
IF User Confirms:
         ↓
suspendUser(userId)
         ↓
POST /api/admin/users/{userId}/suspend
         ↓
┌────────────────────────────────────────────────────────────────┐
│  RESPONSE                                                      │
├────────────────────────────────────────────────────────────────┤
│  Success: {success: true, status: "suspended"}                 │
│                                                                 │
│  Error: {detail: "User is already suspended"}                  │
└────────────────────────────────────────────────────────────────┘
         ↓
┌────────────────────────────────────────────────────────────────┐
│  RESULT                                                        │
├────────────────────────────────────────────────────────────────┤
│  Success:                                                      │
│  - alert('User suspended successfully')                        │
│  - loadUsersPage()  → Page reloads                             │
│  - User shows status: "Suspended" (Red badge)                  │
│  - Button changes from "Suspend" to "Activate"                 │
│                                                                 │
│  Error:                                                        │
│  - alert('Failed to suspend user: ...')                        │
│  - Page state unchanged                                        │
└────────────────────────────────────────────────────────────────┘
```

---

## ✅ Activate User Flow

```
┌─────────────────────────────────────────────────────────────────┐
│               ACTIVATE USER FLOW                                │
└─────────────────────────────────────────────────────────────────┘

User Clicks "Activate" Button (for suspended user)
         ↓
activateUser(userId)
         ↓
POST /api/admin/users/{userId}/activate
         ↓
┌────────────────────────────────────────────────────────────────┐
│  RESPONSE                                                      │
├────────────────────────────────────────────────────────────────┤
│  Success: {success: true, status: "active"}                    │
│                                                                 │
│  Error: {detail: "User is already active"}                     │
└────────────────────────────────────────────────────────────────┘
         ↓
┌────────────────────────────────────────────────────────────────┐
│  RESULT                                                        │
├────────────────────────────────────────────────────────────────┤
│  Success:                                                      │
│  - alert('User activated successfully')                        │
│  - loadUsersPage()  → Page reloads                             │
│  - User shows status: "Active" (Green badge)                   │
│  - Button changes from "Activate" to "Suspend"                 │
│                                                                 │
│  Error:                                                        │
│  - alert('Failed to activate user: ...')                       │
│  - Page state unchanged                                        │
└────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Complete User Lifecycle

```
┌─────────────────────────────────────────────────────────────────┐
│           COMPLETE USER MANAGEMENT LIFECYCLE                    │
└─────────────────────────────────────────────────────────────────┘

1. LOAD USERS
   ├─ GET /api/admin/users
   ├─ Display in table
   └─ Store in window.allUsers

2. SEARCH/FILTER
   ├─ User types in search
   ├─ filterUsers() filters window.allUsers
   └─ Table updates live

3. VIEW DETAILS
   ├─ User clicks "View"
   ├─ GET /api/admin/users/{id}
   └─ Show modal with user data

4. UPDATE TIER (from modal)
   ├─ Select new tier
   ├─ Click "Update Tier"
   ├─ PUT /api/admin/users/{id}/tier
   ├─ Show success
   ├─ Close modal
   └─ Reload users table

5. SUSPEND/ACTIVATE (from table)
   ├─ Click "Suspend" or "Activate"
   ├─ Show confirmation
   ├─ POST /api/admin/users/{id}/suspend/activate
   ├─ Show result
   └─ Reload users table

6. REPEAT
   └─ Search, view, edit, action as needed
```

---

## 📡 API Call Timeline (Concurrent)

```
Timeline for Overview Page Load:

t=0ms   └─ loadOverviewPage() starts
        │
t=50ms  ├─ GET /api/admin/stats/overview (starts)
        ├─ GET /api/admin/activity (starts)
        │
t=100ms │  (API calls in progress)
        │
t=200ms ├─ GET /api/admin/stats/overview (returns) ✓
        ├─ GET /api/admin/activity (returns) ✓
        │  Render stats cards
        │  Render activity table
        │
t=300ms ├─ loadRevenueBreakdown() starts
        ├─ GET /api/admin/revenue/breakdown (starts)
        │
t=400ms ├─ GET /api/admin/revenue/breakdown (returns) ✓
        │  Render revenue cards
        │
t=450ms └─ Page fully rendered ✓

Total Time: ~450ms (with network latency)
```

---

## 🎯 Error Recovery Flow

```
┌─────────────────────────────────────────────────────────────────┐
│               ERROR HANDLING & RECOVERY                         │
└─────────────────────────────────────────────────────────────────┘

API Call Fails
         ↓
catch (error) block executes
         ↓
┌────────────────────────────────────────────────────────────────┐
│  SHOW ERROR STATE                                              │
├────────────────────────────────────────────────────────────────┤
│  For Overview/Users Page:                                      │
│  <div class="error-state">                                     │
│    <i class="fas fa-exclamation-circle"></i>                  │
│    <h3>Failed to Load [Page Name]</h3>                         │
│    <p>${error.message}</p>                                     │
│    <button onclick="load[PageName]Page()">Retry</button>       │
│  </div>                                                        │
│                                                                 │
│  For Modal Actions:                                            │
│  alert('Failed to [action]: ' + error.message)                 │
└────────────────────────────────────────────────────────────────┘
         ↓
User Sees Error Message
         ↓
User Can Click "Retry" or Try Action Again
         ↓
New API Call Attempts
         ↓
Success or Error Again (cycle continues)
```

---

**Data Flow Diagrams Complete ✅**

All flows verified and working correctly.
